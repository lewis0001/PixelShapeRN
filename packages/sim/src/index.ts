/**
 * @botforge/sim — three.js + Rapier browser simulator. Phase-0 placeholder;
 * real implementation lands in Phase 3.
 */

/** Fixed physics timestep rate for the simulator, in Hz. */
export const SIM_FIXED_STEP_HZ = 120;
