/**
 * @botforge/shared — TypeScript types generated from the project's JSON
 * Schemas. Phase-0 placeholder; generated types land in Phase 1.
 */

/** Unique identifier for a robot. */
export type RobotId = string;

/** Current version of the shared JSON Schemas. */
export const SCHEMA_VERSION = 1;
