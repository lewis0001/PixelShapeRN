{% include "_frontmatter.j2" %}

All of {{ robot.name }}'s printed parts are designed to print **without
supports**: no overhang tricks, no rafts, no glue. Any well-tuned FDM printer
with a 0.4 mm nozzle can produce them.

## Settings

Use the `{{ profile }}` profile that ships with this robot, or dial in the same
values by hand:

- **Layer height:** 0.2 mm
- **Walls / perimeters:** 3
- **Infill:** 15% gyroid
- **Supports:** none — every part is support-free by design
- Print each part flat on the bed in its default orientation.

{% if not plan_available %}
:::note
This page was generated before the print plan was available — filament and
time estimates below are pending. Re-run the build to fill them in.
:::
{% endif %}

## Parts

{% if parts %}
| Part | Qty | Material | Color role | Filament (per part) | Time (per part) |
| --- | --- | --- | --- | --- | --- |
{% for part in parts %}
| `{{ part.id }}` | {{ part.qty }} | {{ part.material }} | {{ part.color_role }} | {% if part.grams is not none %}{{ "%.2f"|format(part.grams) }} g{% else %}estimate pending{% endif %} | {% if part.minutes is not none %}{{ part.minutes }} min{% else %}estimate pending{% endif %} |
{% endfor %}
{% else %}
This robot has no printed parts.
{% endif %}

The **color role** column tells you which filament color each part uses: keep
all `body` parts one color and all `accent` parts another, and the robot will
look the way it does on the site — but this is your build, so any palette goes.

{% if totals_grams is not none or totals_minutes is not none %}
## Totals

{% if totals_grams is not none %}- Filament: about **{{ "%.2f"|format(totals_grams) }} g** across all parts.
{% endif %}
{% if totals_minutes is not none %}- Print time: about **{{ totals_minutes }} minutes** of printing.
{% endif %}
{% endif %}

When everything is off the bed, check each part against the photos in the
[assembly guide](./assemble/), then move on to [wiring](./wiring/).
